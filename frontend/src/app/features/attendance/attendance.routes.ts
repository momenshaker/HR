import { Routes } from '@angular/router';
import { AttendancePageComponent } from './attendance.component';

export const ATTENDANCE_ROUTES: Routes = [
  { path: '', component: AttendancePageComponent }
];
