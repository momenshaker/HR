import { Routes } from '@angular/router';
import { OrganizationsPageComponent } from './organizations.component';

export const ORGANIZATIONS_ROUTES: Routes = [
  { path: '', component: OrganizationsPageComponent }
];
