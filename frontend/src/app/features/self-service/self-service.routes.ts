import { Routes } from '@angular/router';
import { SelfServicePageComponent } from './self-service.component';

export const SELF_SERVICE_ROUTES: Routes = [
  {
    path: '',
    component: SelfServicePageComponent
  }
];
