import { Routes } from '@angular/router';
import { AnnouncementsPageComponent } from './announcements.component';

export const ANNOUNCEMENTS_ROUTES: Routes = [
  { path: '', component: AnnouncementsPageComponent }
];
