import { Routes } from '@angular/router';
import { LeaveRequestsPageComponent } from './leave.component';

export const LEAVE_ROUTES: Routes = [
  { path: '', component: LeaveRequestsPageComponent }
];
